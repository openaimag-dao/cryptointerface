fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
httpx==0.28.1
websockets==13.1

# Every AI-powered feature (app/services/gemini_client.py — chat,
# translation, digest, processing, explanation). The AI Decision Engine
# itself stays deterministic/no-LLM, see AI_ENGINE.md. Bumps httpx to
# 0.28.1 (a google-genai dependency) — verified the whole suite still
# passes on it.
google-genai==1.75.0

# News Engine (RSS parsing — app/intelligence/news/)
feedparser==6.0.11

# Data engine
sqlalchemy==2.0.35
asyncpg==0.29.0
redis==5.0.8
numpy==1.26.4

# Auth (app/services/auth_service.py) — real accounts for the private
# dashboard/admin panel, not the News Portal (which stays public).
bcrypt==4.2.0
pyjwt==2.9.0
email-validator==2.2.0

# Dev / test
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
ruff==0.7.0
